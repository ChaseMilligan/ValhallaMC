{
  "name": "valhallamc",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node build-index.js",
    "deploy": "npx wrangler pages deploy",
    "fulldeploy": "npm run build && git add . && git commit -m 'content build' && git push origin main && npx wrangler pages deploy",
    "dev": "npx wrangler pages dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@tebexio/tebex.js": "^1.9.0",
    "dotenv": "^17.2.3",
    "wrangler": "^4.60.0"
  }
}
